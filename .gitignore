node_modules/
dist/
*.local
.vite/
.DS_Store
*.tsbuildinfo

# 팀에서 받은 모델 패키지 — 체크포인트(.pt)와 zip이 수백 MB라 git에 넣지 않는다.
# 모델 파일은 별도로 공유하고, 이 저장소는 UI 소스만 담는다.
wafer_final_package/
wafer_final_package.zip
*.pt
*.zip

# esbuild가 Windows에서 /dev/null을 실제 파일로 만들어 버린 흔적
dev/
